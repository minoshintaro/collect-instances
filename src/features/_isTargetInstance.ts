// export const isTargetInstance = (node: InstanceNode): boolean => {
//   if (!node.visible) return false;
//   let parent = node.parent;
//   while (parent) {
//     if (parent.type === 'COMPONENT' || parent.type === 'INSTANCE') return false;
//     parent = parent.parent;
//   }
//   return true;
// }
